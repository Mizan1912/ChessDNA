# Copy this file to .env and fill in real values. Never commit .env itself.
#
# Nothing is needed yet in Phase 0-2 — no login, no database, no engine calls
# leave this repo. Variables below are added here only when the phase that
# needs them actually starts, so this file never lists a secret before there
# is code that reads it.
