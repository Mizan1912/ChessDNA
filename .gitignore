# dependencies
node_modules/

# env files - never commit real secrets
.env
.env.local
.env.*.local

# build output
frontend/dist/
.vercel/

# editor / os
.DS_Store
Thumbs.db
.claude/settings.local.json

# logs
*.log

# agent scratch space (screenshots, throwaway scripts used for self-verification)
.scratch/

# Stockfish engine files, copied out of node_modules on postinstall — see
# frontend/scripts/copy-stockfish.js. No need for a 1.8MB binary in git.
frontend/public/stockfish/
